# 🏗️ Задание №9: Архитектурные Шаблоны Проектирования

Полный набор примеров архитектурных паттернов проектирования с демонстрацией их преимуществ и практического применения.

## 📋 Цель Задания

Научиться применять паттерны проектирования в реальных проектах и понимать их преимущества.

## 🎯 Шаги Выполнения

### ✅ Шаг 1: Выбор Подходящего Паттерна
**Выбранные паттерны:**
- **Factory Method** - для создания различных типов объектов
- **Singleton** - для обеспечения единственного экземпляра класса
- **Decorator** - для динамического добавления функциональности
- **MVC** - для разделения логики, представления и контроллера

### ✅ Шаг 2: Модификация Существующей Функциональности
Каждый паттерн применён на реальной задаче с "до" и "после" версиями.

### ✅ Шаг 3: Доказательство Преимуществ
Сравнение кода, метрики, анализ улучшений.

## 📁 Структура Проекта

```
design-patterns-implementation/
├── README.md                          # Этот файл
├── requirements.txt                   # Зависимости проекта
├── PATTERNS_GUIDE.md                  # Подробное руководство по паттернам
│
├── patterns/
│   ├── __init__.py
│   ├── factory_method/
│   │   ├── __init__.py
│   │   ├── before.py                 # ❌ Без паттерна
│   │   ├── after.py                  # ✅ С паттерном Factory Method
│   │   └── comparison.py              # 📊 Сравнение и бенчмарк
│   │
│   ├── singleton/
│   │   ├── __init__.py
│   │   ├── before.py                 # ❌ Без паттерна (множество экземпляров)
│   │   ├── after.py                  # ✅ С паттерном Singleton
│   │   └── comparison.py              # 📊 Сравнение
│   │
│   ├── decorator/
│   │   ├── __init__.py
│   │   ├── before.py                 # ❌ Без паттерна (наследование)
│   │   ├── after.py                  # ✅ С паттерном Decorator
│   │   └── comparison.py              # 📊 Сравнение
│   │
│   └── mvc/
│       ├── __init__.py
│       ├── before.py                 # ❌ Без разделения (спагетти-код)
│       ├── after.py                  # ✅ С паттерном MVC
│       └── comparison.py              # 📊 Сравнение
│
├── examples/
│   ├── __init__.py
│   ├── vps_provider_system.py         # 🔹 Factory Method: система VPS провайдеров
│   ├── database_connection.py         # 🔹 Singleton: подключение к БД
│   ├── notification_system.py         # 🔹 Decorator: система уведомлений
│   └── todo_app.py                    # 🔹 MVC: приложение TODO списка
│
├── tests/
│   ├── __init__.py
│   ├── test_factory.py
│   ├── test_singleton.py
│   ├── test_decorator.py
│   └── test_mvc.py
│
├── benchmarks/
│   ├── __init__.py
│   ├── benchmark_patterns.py          # Бенчмарки производительности
│   └── results/
│       └── comparison_report.md       # Отчёт сравнения
│
└── docs/
    ├── factory_method.md              # Подробное описание Factory Method
    ├── singleton.md                   # Подробное описание Singleton
    ├── decorator.md                   # Подробное описание Decorator
    └── mvc.md                         # Подробное описание MVC
```

## 🚀 Быстрый Старт

### Установка
```bash
# Клонирование репозитория
git clone https://github.com/plurexoff/design-patterns-implementation.git
cd design-patterns-implementation

# Создание виртуального окружения
python -m venv venv

# Активация окружения
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### Запуск Примеров

#### 1. Factory Method - Система VPS Провайдеров
```bash
# Просмотр "до" версии
python -m patterns.factory_method.before

# Просмотр "после" версии с Factory Method
python -m patterns.factory_method.after

# Сравнение и анализ
python -m patterns.factory_method.comparison

# Реальный пример: VPS система
python -m examples.vps_provider_system
```

#### 2. Singleton - Подключение к БД
```bash
# Просмотр "до" версии
python -m patterns.singleton.before

# Просмотр "после" версии
python -m patterns.singleton.after

# Сравнение
python -m patterns.singleton.comparison

# Реальный пример
python -m examples.database_connection
```

#### 3. Decorator - Система Уведомлений
```bash
# Просмотр "до" версии
python -m patterns.decorator.before

# Просмотр "после" версии
python -m patterns.decorator.after

# Сравнение
python -m patterns.decorator.comparison

# Реальный пример
python -m examples.notification_system
```

#### 4. MVC - TODO Приложение
```bash
# Просмотр "до" версии
python -m patterns.mvc.before

# Просмотр "после" версии
python -m patterns.mvc.after

# Сравнение
python -m patterns.mvc.comparison

# Реальный пример
python -m examples.todo_app
```

### Запуск Тестов
```bash
# Все тесты
pytest tests/

# Конкретный паттерн
pytest tests/test_factory.py
pytest tests/test_singleton.py
pytest tests/test_decorator.py
pytest tests/test_mvc.py
```

### Бенчмарк Производительности
```bash
python -m benchmarks.benchmark_patterns
```

## 📚 Описание Паттернов

### 1️⃣ Factory Method

**Назначение:** Создание объектов без указания их конкретных классов.

**Проблема (ДО):**
```python
# Клиент знает все типы и создаёт их напрямую
if provider_type == "aws":
    vps = AWS_VPS(name, config)
elif provider_type == "azure":
    vps = Azure_VPS(name, config)
elif provider_type == "gcp":
    vps = GCP_VPS(name, config)
# ❌ Хардкод логики создания
# ❌ Сложно добавлять новые типы
# ❌ Нарушение принципа Open/Closed
```

**Решение (ПОСЛЕ):**
```python
# Factory инкапсулирует логику создания
class VPSFactory:
    @staticmethod
    def create(provider_type, name, config):
        creators = {
            "aws": lambda: AWS_VPS(name, config),
            "azure": lambda: Azure_VPS(name, config),
            "gcp": lambda: GCP_VPS(name, config),
        }
        return creators[provider_type]()

vps = VPSFactory.create("aws", "server1", config)
# ✅ Логика создания централизована
# ✅ Легко добавлять новые типы
# ✅ Клиент не знает об конкретных классах
```

**Преимущества:**
- 🎯 **Инкапсуляция логики создания** - вся логика в одном месте
- 🔄 **Легко расширять** - добавление новых типов без изменения клиента
- 📦 **Слабая связанность** - клиент не зависит от конкретных классов
- ✅ **Следование SOLID** - Open/Closed, Dependency Inversion

**Применение:**
```python
# Реальный пример: Система VPS провайдеров
vps_aws = VPSFactory.create("aws", "web-server", aws_config)
vps_azure = VPSFactory.create("azure", "app-server", azure_config)
vps_gcp = VPSFactory.create("gcp", "db-server", gcp_config)
```

---

### 2️⃣ Singleton

**Назначение:** Обеспечение единственного экземпляра класса во всей программе.

**Проблема (ДО):**
```python
# Класс создаётся множество раз
db1 = Database("localhost", 5432)
db2 = Database("localhost", 5432)
db3 = Database("localhost", 5432)

print(db1 is db2)  # False ❌ Разные объекты!
# ❌ Множественные подключения к БД
# ❌ Утечка ресурсов
# ❌ Непредсказуемое поведение
```

**Решение (ПОСЛЕ):**
```python
class Database(Singleton):
    def __init__(self, host, port):
        self.host = host
        self.port = port
        self.connection = None

db1 = Database.get_instance("localhost", 5432)
db2 = Database.get_instance("localhost", 5432)
db3 = Database.get_instance("localhost", 5432)

print(db1 is db2)  # True ✅ Один и тот же объект!
# ✅ Единственное подключение к БД
# ✅ Экономия ресурсов
# ✅ Гарантированное единообразное состояние
```

**Преимущества:**
- 🔐 **Контроль доступа** - единственная точка доступа к ресурсу
- 💾 **Экономия ресурсов** - одно подключение вместо многих
- 🔄 **Глобальное состояние** - все компоненты используют один объект
- 🛡️ **Потокобезопасность** - гарантия при правильной реализации

**Применение:**
```python
# Логирование
logger = Logger.get_instance()
logger.log("Application started")
logger.log("Processing request")

# Кэширование
cache = Cache.get_instance()
cache.set("user:1", user_data)

# Конфигурация
config = Config.get_instance()
db_url = config.get("DATABASE_URL")
```

---

### 3️⃣ Decorator

**Назначение:** Динамическое добавление функциональности к объектам.

**Проблема (ДО):**
```python
# Иерархия наследования разрастается
class EmailNotification:
    def send(self): ...

class EmailWithSMSNotification(EmailNotification):
    def send(self): ...

class EmailWithSMSAndSlackNotification(EmailWithSMSNotification):
    def send(self): ...

# ❌ Экспоненциальный рост классов (2^n комбинаций)
# ❌ Жёсткая иерархия
# ❌ Сложно комбинировать функциональность
```

**Решение (ПОСЛЕ):**
```python
# Decorator добавляет функциональность динамически
class NotificationDecorator:
    def __init__(self, notification):
        self.notification = notification

class SMSDecorator(NotificationDecorator):
    def send(self):
        self.notification.send()
        self.send_sms()

class SlackDecorator(NotificationDecorator):
    def send(self):
        self.notification.send()
        self.send_slack()

# Гибкое комбинирование
notif = EmailNotification()
notif = SMSDecorator(notif)
notif = SlackDecorator(notif)
notif.send()  # Email -> SMS -> Slack
# ✅ Линейный рост сложности
# ✅ Гибкая комбинация
# ✅ Открыт/Закрыт принцип (Open/Closed)
```

**Преимущества:**
- 🔗 **Композиция вместо наследования** - больше гибкости
- 📚 **Комбинируемость** - легко комбинировать декораторы
- 🔀 **Динамическое добавление** - функциональность добавляется в runtime
- ⚙️ **Разделение ответственности** - каждый декоратор отвечает за одно

**Применение:**
```python
# Система уведомлений
notif = EmailNotification(message)
if user.has_sms:
    notif = SMSDecorator(notif)
if user.has_slack:
    notif = SlackDecorator(notif)
if user.has_telegram:
    notif = TelegramDecorator(notif)
notif.send()
```

---

### 4️⃣ MVC

**Назначение:** Разделение кода на три независимых слоя.

**Проблема (ДО):**
```python
# Всё в одном месте - спагетти-код
class TodoApp:
    def __init__(self):
        self.todos = []
    
    def add_todo(self, title):
        todo = {"id": len(self.todos), "title": title, "done": False}
        self.todos.append(todo)
        print(f"✅ Added: {title}")  # View логика
    
    def show_todos(self):
        for todo in self.todos:  # Model логика
            status = "✓" if todo["done"] else " "
            print(f"[{status}] {todo['title']}")  # View логика
    
    def mark_done(self, todo_id):
        self.todos[todo_id]["done"] = True  # Model логика
        print(f"✅ Marked done")  # View логика

# ❌ Смешанная логика
# ❌ Сложно тестировать
# ❌ Сложно менять отображение
```

**Решение (ПОСЛЕ):**
```python
# Model - управление данными
class Todo:
    def __init__(self, title):
        self.title = title
        self.done = False

class TodoModel:
    def __init__(self):
        self.todos = []
    
    def add(self, title):
        self.todos.append(Todo(title))
    
    def get_all(self):
        return self.todos
    
    def mark_done(self, index):
        self.todos[index].done = True

# View - отображение данных
class TodoView:
    def show_todos(self, todos):
        for i, todo in enumerate(todos):
            status = "✓" if todo.done else " "
            print(f"[{status}] {todo.title}")
    
    def show_added(self, title):
        print(f"✅ Added: {title}")

# Controller - обработка действий пользователя
class TodoController:
    def __init__(self, model, view):
        self.model = model
        self.view = view
    
    def add_todo(self, title):
        self.model.add(title)
        self.view.show_added(title)
    
    def show_todos(self):
        todos = self.model.get_all()
        self.view.show_todos(todos)
    
    def mark_done(self, index):
        self.model.mark_done(index)

# Использование
model = TodoModel()
view = TodoView()
controller = TodoController(model, view)

controller.add_todo("Learn Design Patterns")
controller.add_todo("Apply MVC")
controller.show_todos()

# ✅ Разделённая логика
# ✅ Легко тестировать
# ✅ Легко менять отображение
```

**Преимущества:**
- 🔀 **Разделение ответственности** - каждый слой отвечает за своё
- 🧪 **Тестируемость** - каждый слой тестируется отдельно
- ♻️ **Переиспользуемость** - Model может работать с разными View
- 🎨 **Гибкость** - можно менять представление без изменения логики

**Применение:**
```python
# TODO приложение
model = TodoModel()
view = TodoView()
controller = TodoController(model, view)

# Можно легко создать другое View
class JsonView:
    def show_todos(self, todos):
        return json.dumps([{"title": t.title, "done": t.done} for t in todos])

json_view = JsonView()
json_controller = TodoController(model, json_view)
```

## 📊 Сравнение Паттернов

| Паттерн | Назначение | Плюсы | Минусы | Когда использовать |
|---------|-----------|-------|--------|-------------------|
| **Factory Method** | Создание объектов | Инкапсуляция, расширяемость | Доп. класс | Много типов создаваемых объектов |
| **Singleton** | Единственный экземпляр | Контроль ресурсов | Глобальное состояние | БД, логирование, конфиг |
| **Decorator** | Добавление функциональности | Гибкость, композиция | Сложность при стеке | Опциональная функциональность |
| **MVC** | Архитектура приложения | Тестируемость, модульность | Overhead кода | Крупные приложения |

## 🧪 Тестирование

Каждый паттерн поставляется с юнит-тестами:

```bash
# Factory Method
pytest tests/test_factory.py -v

# Singleton
pytest tests/test_singleton.py -v

# Decorator
pytest tests/test_decorator.py -v

# MVC
pytest tests/test_mvc.py -v
```

## 📈 Бенчмарк

Сравнение производительности "до" и "после" применения паттернов:

```bash
python -m benchmarks.benchmark_patterns
```

Результаты сохраняются в `benchmarks/results/comparison_report.md`

## 📖 Дополнительные Ресурсы

- [Design Patterns: Elements of Reusable Object-Oriented Software](https://en.wikipedia.org/wiki/Design_Patterns) - классическая книга
- [Refactoring.Guru - Design Patterns](https://refactoring.guru/design-patterns) - интерактивный гайд
- [SOLID принципы](https://en.wikipedia.org/wiki/SOLID) - основа хорошего дизайна

## 📝 Чек-лист Выполнения

- ✅ Выбраны 4 паттерна (Factory, Singleton, Decorator, MVC)
- ✅ Для каждого паттерна создана версия "до" и "после"
- ✅ Показаны преимущества на конкретных примерах
- ✅ Добавлены тесты
- ✅ Добавлены бенчмарки
- ✅ Документация

## 👨‍💻 Автор

Создано для задания по применению архитектурных паттернов проектирования.

---

**Happy coding with patterns! 🚀**
