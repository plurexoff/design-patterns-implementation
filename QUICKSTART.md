# 🚀 QUICKSTART Guide

Обычная экспресс-университета по четырём архитектурным паттернам.

## 🎉 5-минутный старт

### Шаг 1: Клонируйте репозиторий
```bash
git clone https://github.com/plurexoff/design-patterns-implementation.git
cd design-patterns-implementation
```

### Шаг 2: Нет тех зависимостей!
Код использует только стандартную библиотеку Python 😀

### Шаг 3: Запустите примеры

## 📚 Таблица быстрых команд

| Паттерн | "ДО" | "ПОСЛЕ" | Сравнение |
|---------|----------|----------|---------------|
| **Factory Method** | `python -m patterns.factory_method.before` | `python -m patterns.factory_method.after` | `python -m patterns.factory_method.comparison` |
| **Singleton** | `python -m patterns.singleton.before` | `python -m patterns.singleton.after` | `python -m patterns.singleton.comparison` |
| **Decorator** | `python -m patterns.decorator.before` | `python -m patterns.decorator.after` | `python -m patterns.decorator.comparison` |
| **MVC** | `python -m patterns.mvc.before` | `python -m patterns.mvc.after` | `python -m patterns.mvc.comparison` |

---

## 🏭 Factory Method - Система VPS провайдеров

### Шаг 1: Видет проблему (hardcoded logic)
```bash
python -m patterns.factory_method.before
```

Проблемы:
- 📁 От создание зависит от конкретных классов
- ⚖️ При добавлении провайдера снежда бежит функцию
- 📁 Нарушение Open/Closed принципа

### Шаг 2: Видит решение (Factory)
```bash
python -m patterns.factory_method.after
```

Преимущества:
- 🌟 Инкапсуляция логики создания
- 🦾 Добавление провайдера за 2 строки
- ✅ Соответствие SOLID

### Шаг 3: Сравните и анализируйте
```bash
python -m patterns.factory_method.comparison
```

Будут отображены:
- 📊 Подробная таблица сравнения
- 🚀 Тесты производительности
- 🦾 Метрики сложности

---

## 🔐 Singleton - Подключение к БД

### Шаг 1: Неэффективные множественные инстанцы
```bash
python -m patterns.singleton.before
```

Проблемы:
- 💾 Множественные экземпляры = крад ресурсов
- ⚠️ Непоследовательность стоостояния
- 💾 Утечки памяти

### Шаг 2: Единственный экземпляр
```bash
python -m patterns.singleton.after
```

Преимущества:
- 🌟 Одна то одна то одна объекта
- 🌸 Оптимальное использование ресурсов
- 🦾 Гарантированное нервные стоостояние

### Шаг 3: Нсстраивайте
```bash
python -m patterns.singleton.comparison
```

---

## 🎪 Decorator - Система уведомлений

### Шаг 1: Всплывки классов (2^n)
```bash
python -m patterns.decorator.before
```

Проблемы:
- 🚀 Жесткая иеархия
- 💾 Энсподенциальный рост комбинаций
- 📁 Громоздкая наследование

### Шаг 2: Гибкая композиция
```bash
python -m patterns.decorator.after
```

Преимущества:
- 🌟 Неограниченные комбинации
- 🦾 Динамическая композиция
- 🌸 Линейный рост сложности

### Шаг 3: Сравните архитектуры
```bash
python -m patterns.decorator.comparison
```

---

## 🏢 MVC - TODO Приложение

### Шаг 1: Монолитная классов (спагетти-код)
```bash
python -m patterns.mvc.before
```

Проблемы:
- 📁 Все в одном классе
- ⚠️ Смешание этих трех дел
- ❌ Тестирование сложно

### Шаг 2: Нячно архитектурное разделение
```bash
python -m patterns.mvc.after
```

Преимущества:
- ✅ Model = логика
- ✅ View = дисплей
- ✅ Controller = врээтки
- 🦾 Отдельно тестируемые этапы

### Шаг 3: Архитектурные бенефиции
```bash
python -m patterns.mvc.comparison
```

---

## 🌟 Что дальше?

### 📖 Полная документация
```bash
ls docs/
```

### 🎮 Понразимые примеры
```bash
ls examples/
```

### 🦾 Юнит-тесты
```bash
pytest tests/ -v
```

---

## 🚀 Основные выводы

| Паттерн | Главная цель | Главная выгода |
|---------|-------------|------------|
| **Factory** | Отдел создание | Гибкость |
| **Singleton** | Гарантир 1 экз | Оптимизация |
| **Decorator** | Адд функциональность | Композиция |
| **MVC** | Разделение слоёв | Помжлдование |

---

**Happy learning! 🚀**
